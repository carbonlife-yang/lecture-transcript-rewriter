# 钥匙管理 SOP · 5 分钟上手

> **主讲师 5 分钟就能上手的钥匙管理指南**。一步步教你:怎么生成邀请码、怎么发给别人、怎么撤销、怎么追踪。

---

## 🎯 你能做什么

| 你想做的事 | 用什么 | 在哪个文件 |
|---|---|---|
| **新增被邀请人** | 生成邀请码 | `KEYS.md` |
| **撤销某人的权限** | 改状态 | `KEYS.md` |
| **看谁在用** | 看活跃用户 + 统计 | `KEYS.md` + `USAGE_LOG.md` |
| **看使用详情** | 看每次记录 | `USAGE_LOG.md` |
| **导出数据分析** | grep/awk 命令 | 终端 |

---

## 步骤 1:首次设置(5 分钟)

### 1.1 打开 KEYS.md

如果你看到这份 SOP 但还没创建 KEYS.md:

```bash
# 复制模板
cp KEYS.md KEYS.md.backup   # 备份
# 然后手动编辑 KEYS.md
```

如果你已经有 KEYS.md,跳过这步。

### 1.2 填主讲师信息

在 KEYS.md 顶部加上:

```markdown
## 主讲师信息

| 项目 | 值 |
|------|---|
| 主讲师姓名 | 张三(化名) |
| 创建时间 | 2026-08-05 |
| 联系邮箱 | zhangsan@xx.com |
| Skill 部署位置 | /workspace/.skills/lecture-transcript-rewriter/ |
```

### 1.3 生成 5 个初始邀请码

**方法 A:用 Python(推荐)**

```bash
python3 -c "
import random
chars = 'ABCDEFGHJKMNPQRSTUVWXYZ23456789'
for i in range(5):
    code = 'SKL-' + ''.join(random.choices(chars, k=4)) + '-' + ''.join(random.choices(chars, k=4))
    print(code)
"
```

**方法 B:用 generate_keys.py 脚本**

先创建脚本:
```bash
cat > generate_keys.py << 'EOF'
import random

chars = 'ABCDEFGHJKMNPQRSTUVWXYZ23456789'
n = int(input('生成几个邀请码? '))
for _ in range(n):
    code = 'SKL-' + ''.join(random.choices(chars, k=4)) + '-' + ''.join(random.choices(chars, k=4))
    print(code)
EOF
```

再运行:
```bash
python3 generate_keys.py
# 输入 5
# 输出 5 个邀请码
```

**方法 C:手动编**(不推荐,容易重复)

格式:`SKL-XXXX-XXXX`,4 位大写字母 + 数字。

### 1.4 把邀请码填到 KEYS.md

打开 KEYS.md,在"待激活邀请码"表里加:

```markdown
### 待激活邀请码(剩余 K 个)

| 邀请码 | 生成时间 | 状态 | 给谁了 |
|--------|----------|------|--------|
| SKL-A1B2-3C4D | 2026-08-05 | 待激活 | — |
| SKL-E5F6-7G8H | 2026-08-05 | 待激活 | — |
| SKL-M3N4-5P6Q | 2026-08-05 | 待激活 | — |
| SKL-R7S8-9T0U | 2026-08-05 | 待激活 | — |
| SKL-V1W2-3X4Y | 2026-08-05 | 待激活 | — |
```

**完成首次设置!** 现在可以发邀请码了。

---

## 步骤 2:分享给被邀请人(2 分钟/次)

### 2.1 选个邀请码

从 KEYS.md 复制一个"待激活"状态的邀请码。

### 2.2 告诉被邀请人 3 件事

**示例话术**(微信/邮件/口头都行):

> "我有个多维表格改稿的 skill,你拿去用。
>
> 邀请码:SKL-A1B2-3C4D
>
> 用法:第一次用的时候,告诉 AI 助手你的邀请码 + 邮箱,以后直接用就行。
>
> 有问题找我。"

### 2.3 标记"已发给谁"

打开 KEYS.md,在"待激活邀请码"表里,把"给谁了"列填上:

```markdown
| SKL-A1B2-3C4D | 2026-08-05 | 待激活 | 已发给 zhang@xx.com(微信 8/5) |
```

**完成分享!** 等被邀请人激活。

---

## 步骤 3:被邀请人激活(自动,不用你管)

被邀请人第一次用 skill 时,会输入:
> "我要用 skill,邀请码 SKL-A1B2-3C4D,我的邮箱 zhang@xx.com"

**AI 助手自动做**:
1. 检查邀请码是否存在 ✓
2. 检查邮箱是否已注册 ✓ (没注册过)
3. 把邀请码移到"活跃用户"表
4. 记录首次使用时间

激活后,你打开 KEYS.md 能看到:
- "待激活"区少一个码
- "活跃"区多一个用户

---

## 步骤 4:看使用情况(日常)

### 4.1 看谁在用

打开 KEYS.md,看"活跃用户"表的"最近使用"和"使用次数"。

**判断用户活跃度**:
- 最近使用 < 7 天前:高频活跃
- 7-30 天:正常活跃
- 30-60 天:低频,可能流失
- > 60 天:几乎不用,考虑撤销

### 4.2 看用了什么

打开 USAGE_LOG.md,看每次用了什么模板 + 用了多久。

**判断模板热度**:
- 哪个模板最常用 → 重点打磨
- 哪个模板没人用 → 考虑删/改

### 4.3 终端命令快速查

```bash
# 看总使用次数
grep -c "成功" USAGE_LOG.md

# 看最活跃用户
grep "^|" USAGE_LOG.md | awk -F'|' '{print $3}' | sort | uniq -c | sort -rn | head -5

# 看最常用模板
grep "^|" USAGE_LOG.md | awk -F'|' '{print $5}' | sort | uniq -c | sort -rn

# 看最近一周的使用
grep "^| $(date -d '7 days ago' +%Y-%m-%d)" USAGE_LOG.md
```

---

## 步骤 5:撤销权限(场景 4 种)

### 场景 A:某员工离职/调岗

**步骤**:
1. 打开 KEYS.md
2. 在"活跃用户"表里,找到这个人
3. 整行剪切到"已撤销用户"表
4. 在"撤销时间"列填今天
5. 在"撤销原因"列填"离职"或"调岗"
6. 保存

**示例**:
```markdown
### 活跃用户(N 个)

(把这一行删掉 ↓)
| 3 | SKL-I9J0-1K2L | wang@xx.com | 活跃 | 2026-08-04 | 2026-08-04 | 1 | — |

### 已撤销用户(M 个)

(加这一行 ↓)
| 1 | SKL-I9J0-1K2L | wang@xx.com | 2026-08-05 | 离职(转去销售部) |
```

**撤销后**:这个人再用 skill,AI 会拒绝并提示"权限已撤销"。

### 场景 B:某讲师长期不用(60 天+)

同上操作,撤销原因填"长期不用(60 天+)"。

### 场景 C:某人滥用了(高频异常)

1. 撤销(同上)
2. 撤销原因填"高频异常:1 天用 10 次"
3. 留档,可能需要再生成新码给别人

### 场景 D:重新激活已撤销的人

1. 找到"已撤销用户"表里这个人
2. 整行剪切回"活跃用户"表
3. 状态改"活跃"
4. 备注列加"(重新激活 YYYY-MM-DD)"

---

## 步骤 6:定期维护(每月 5 分钟)

| 频率 | 动作 | 时间预估 |
|---|---|---|
| **每天** | 看 USAGE_LOG.md 异常监控 | 1 分钟 |
| **每周** | 生成新邀请码(按需) | 1 分钟 |
| **每月** | 撤销长期不用的(60 天+) | 3 分钟 |
| **每季度** | 完整 review,清理已撤销区 | 5 分钟 |
| **每年** | 重置所有邀请码,提高安全性 | 10 分钟 |

---

## 进阶:批量操作

### 批量生成 20 个邀请码

```bash
python3 -c "
import random
chars = 'ABCDEFGHJKMNPQRSTUVWXYZ23456789'
for i in range(20):
    code = 'SKL-' + ''.join(random.choices(chars, k=4)) + '-' + ''.join(random.choices(chars, k=4))
    print(code)
" > new_keys.txt
```

然后把 `new_keys.txt` 的内容粘贴到 KEYS.md 的"待激活"区。

### 批量撤销 60 天+未用的用户

```bash
# 这个命令比较复杂,先简单做
# 1. 打开 KEYS.md
# 2. 看"最近使用"列,挑出 60 天前的
# 3. 手动剪切到"已撤销"区
```

### 导出 CSV 分析

```bash
# 提取最近 100 条使用记录到 CSV
grep "^|" USAGE_LOG.md | head -100 | sed 's/|/,/g' > usage.csv
# 用 Excel 打开 usage.csv
```

---

## 常见问题

### Q1:邀请码被泄露了怎么办?

**A**:立即在 KEYS.md 把这个码改成"已撤销",状态填"泄露"。泄露的码作废,重新生成一个发给原来要发的人。

### Q2:被邀请人没收到邀请码?

**A**:让他看微信/邮件/短信,或者你直接告诉他。

### Q3:被邀请人激活失败,提示"邀请码无效"?

**A**:让他把邀请码复制完整(可能漏字符)。如果还是失败,你把 KEYS.md 的"待激活"区核对一下,码有没有填错。

### Q4:被邀请人想换邮箱(原来填的邮箱错了)?

**A**:在 KEYS.md 里直接改邮箱列。简单粗暴。

### Q5:被邀请人想给第三方用他的码?

**A**:技术上不可行,每个邀请码只能激活 1 个邮箱。如果要给第三方,让他找你要新码,或者直接转发 KEY 给他。

### Q6:USAGE_LOG.md 文件太大了,怎么压缩?

**A**:每月归档一次,把当月记录移到"历史归档"区。或者重置文件,只保留最近 3 个月。

### Q7:能不能在多个 Skill 之间共享 KEYS.md?

**A**:可以!KEYS.md 是独立文件,可以被其他 skill 引用。比如你做个"课程设计 skill",可以用同一份 KEYS.md。

### Q8:KEYS.md 万一被误删了怎么办?

**A**:你必须定期备份。建议每周 cp KEYS.md KEYS.md.backup。USAGE_LOG.md 同理。

### Q9:有没有图形化界面来管理?

**A**:暂时没有(避免引入 UI 复杂度)。如果你需要,建议先手动用 markdown 编辑器管理(比如 VSCode、Typora)。

### Q10:能不能用 git 管理 KEYS.md?

**A**:可以!KEYS.md 是纯文本,完全适合 git 跟踪改动。建议:
```bash
cd /workspace/.skills/lecture-transcript-rewriter/
git init
git add KEYS.md USAGE_LOG.md
git commit -m "初始 KEYS + USAGE_LOG"
# 每次改动后
git add -u
git commit -m "撤销 wang@xx.com(离职)"
```

---

## 最佳实践

### ✅ 推荐
- 每周 review KEYS.md
- 每月归档 USAGE_LOG.md
- 用 git 跟踪 KEYS.md 改动
- 备份 KEYS.md 到多个地方(本地 + 网盘)
- 给邀请码加过期时间(比如 30 天内必须激活)

### ❌ 避免
- 不要把邀请码公开发到网上(微信公众号/知乎)
- 不要用手机号当主标识(易暴露,不可改)
- 不要给所有人"永久"权限(养成撤销习惯)
- 不要把 USAGE_LOG.md 分享出去(含个人使用信息)
- 不要用 AI 自动改 KEYS.md(避免误撤销)

---

## 总结:5 分钟上手路径

1. **生成邀请码** → 用 `generate_keys.py` 一键生成,30 秒
2. **自动填表** → 脚本自动追加到 KEYS.md,0 秒
3. **发给同事** → 复制脚本输出的分享话术,30 秒
4. **看他们激活** → 打开 KEYS.md,1 分钟
5. **看使用情况** → 打开 USAGE_LOG.md,2 分钟

**整套机制,任何讲师 5 分钟就能用。** 🎯

---

## 🚀 超简单版:generate_keys.py 一键搞定

> **这是最简单的方式**。一个 Python 脚本,生成 + 填表 + 分享话术 一次性完成。

### 怎么用

#### 方式 1:交互式(最简单,推荐)

```bash
cd /path/to/lecture-transcript-rewriter/
python3 generate_keys.py
```

然后按提示回答:
- `🔑 要生成几个邀请码? [默认 5]:` → 输入 5(或者直接回车)
- `👤 这批码发给谁?(邮箱/工号/微信名,留空可跳过):` → 输入 zhang@xx.com

**完事了。脚本会**:
1. 生成 5 个邀请码
2. 自动追加到 KEYS.md(标记"已发给 zhang@xx.com")
3. 打印一段**可以直接复制粘贴**的分享话术

#### 方式 2:命令行(更快)

```bash
# 生成 5 个,跳过询问
python3 generate_keys.py 5

# 生成 5 个,并标记发给某人
python3 generate_keys.py 5 --share-to zhang@xx.com

# 生成 10 个
python3 generate_keys.py 10

# 生成 1 个给自己备用
python3 generate_keys.py 1
```

#### 方式 3:不用脚本(超极简)

如果你不想装 Python,直接复制下面这行命令:

```bash
python3 -c "import random; chars='ABCDEFGHJKMNPQRSTUVWXYZ23456789'; [print('SKL-'+''.join(random.choices(chars,k=4))+'-'+''.join(random.choices(chars,k=4))) for _ in range(5)]"
```

输出 5 个邀请码,然后:
1. 手动打开 KEYS.md
2. 粘贴到"待激活邀请码"表
3. 自己编个分享话术发出去

**但还是推荐用脚本**,因为脚本会**自动生成话术** + **自动填表**。

### 实际演示(用脚本)

```bash
$ python3 generate_keys.py
🔑 要生成几个邀请码? [默认 5]: 3
👤 这批码发给谁?(邮箱/工号/微信名,留空可跳过): zhang@xx.com

⏳ 正在生成邀请码...

✅ 成功生成 3 个邀请码:

  1. SKL-29R8-26SJ
  2. SKL-UA8N-UPH6
  3. SKL-MC96-39HD

✅ 已自动追加到 KEYS.md
   并标记"已发给 zhang@xx.com"

======================================================================
📋 分享话术(直接复制发给同事):
======================================================================

Hi zhang@xx.com 👋

我有个讲课逐字稿改写的 skill,你拿去用。

邀请码:SKL-29R8-26SJ / SKL-UA8N-UPH6 / SKL-MC96-39HD

用法(第一次用):
  1. 把 skill 装到你常用的 AI 助手(Claude/Codex/GPT 都行)
  2. 第一次用时,跟 AI 说:"用 lecture-transcript-rewriter skill,我的邀请码是 SKL-29R8-26SJ,我的邮箱是 zhang@xx.com"
  3. 以后直接用,不用再填邀请码

用途:把别人的培训录音/逐字稿改成你自己的版本,40 分钟出第一版。

有问题找我 👋

======================================================================
✅ 复制上面的话术 → 微信/邮件/飞书发给同事
======================================================================
```

**3 步搞定**:输入数字 → 输入邮箱 → 复制话术发送。整个过程 30 秒。

### 脚本特性

| 特性 | 说明 |
|---|---|
| **零依赖** | 只用 Python 标准库,不需要 pip install 任何包 |
| **跨平台** | macOS / Linux / Windows 都能用(Python 3.6+) |
| **防重复** | 自动检查 KEYS.md,如果码已存在会重新生成 |
| **自动填表** | 生成后直接追加到 KEYS.md 的"待激活"表 |
| **标记分享对象** | `--share-to` 参数自动填"给谁了"列 |
| **生成话术** | 输出可以直接复制粘贴的分享话术 |
| **可重复** | 想生成几次都行,每次都是新码 |

### 脚本工作流

```
[运行脚本] → [问几个/发给谁] → [生成码] → [查重] → [追加到 KEYS.md] → [打印话术]
                                                    ↓
                                            [复制话术] → [微信发送]
```

### 完整命令清单

| 场景 | 命令 |
|---|---|
| 交互式生成(默认 5 个) | `python3 generate_keys.py` |
| 一次生成 N 个 | `python3 generate_keys.py N` |
| 生成 N 个并指定分享对象 | `python3 generate_keys.py N --share-to 邮箱` |
| 单独生成 1 个给自己 | `python3 generate_keys.py 1` |
| 看帮助 | `python3 generate_keys.py --help` |
| 完全手动(不装脚本) | `python3 -c "import random; chars='ABCDEFGHJKMNPQRSTUVWXYZ23456789'; [print('SKL-'+''.join(random.choices(chars,k=4))+'-'+''.join(random.choices(chars,k=4))) for _ in range(N)]"` |

### 脚本代码位置

- 完整代码:`lecture-transcript-rewriter/generate_keys.py`
- 大小:6KB(纯 Python 标准库,无依赖)
- 许可证:随便用,可以改

### 如果脚本报错

| 错误信息 | 原因 | 解决 |
|---|---|---|
| `找不到 KEYS.md` | 脚本不在 skill 目录 | `cd` 到 skill 目录再运行 |
| `在 KEYS.md 里找不到"待激活邀请码"表的表头` | KEYS.md 结构不对 | 打开 KEYS.md,确保有"待激活邀请码"表 |
| `SyntaxError` | Python 版本 < 3.6 | 升级 Python |
| `仍有 X 个重复(运气不好)` | 字符集太小,刚好随机到重复 | 再运行一次 |

---

## 分享话术模板(自己改)

如果你想自己写分享话术(不用脚本生成的),可以参考这个模板:

```
Hi [同事名字/邮箱] 👋

我有个讲课逐字稿改写的 skill,你拿去用。

邀请码:[SKL-XXXX-XXXX]

用法(第一次用):
  1. 把 skill 装到你常用的 AI 助手(Claude/Codex/GPT 都行)
  2. 第一次用时,跟 AI 说:"用 lecture-transcript-rewriter skill,我的邀请码是 [码],我的邮箱是 [你的邮箱]"
  3. 以后直接用,不用再填邀请码

用途:把别人的培训录音/逐字稿改成你自己的版本,40 分钟出第一版。

有问题找我 👋
```

**可以根据关系调整**:
- 跟熟同事:`兄弟,试个新东西,码是 XXX,玩得开心`
- 跟半生不熟:`您好,分享一个工具给您,码 XXX`
- 群里发:`@全员 新工具上线,需要码的找我要,一次一个`

---

## 一张图总结整套机制

```
┌─────────────────────────────────────────────┐
│  主讲师                                      │
│  ┌──────────────┐    ┌─────────────────┐    │
│  │generate_keys │───>│ KEYS.md         │    │
│  │     .py      │    │  + 待激活区      │    │
│  └──────┬───────┘    │  + 活跃用户区    │    │
│         │            │  + 已撤销区      │    │
│         │            └─────────────────┘    │
│         v                                    │
│  ┌──────────────────┐                       │
│  │ 分享话术(自动)  │ ───> 微信/邮件/飞书 ───>│
│  └──────────────────┘                       │
└─────────────────────────────────────────────┘
                    │
                    v
┌─────────────────────────────────────────────┐
│  被邀请人                                     │
│  1. 收到邀请码                                │
│  2. 第一次用 skill,输入"邀请码 + 邮箱"        │
│  3. AI 校验 → 激活白名单                      │
│  4. 后续每次用,自动记录到 USAGE_LOG.md        │
└─────────────────────────────────────────────┘
                    │
                    v
┌─────────────────────────────────────────────┐
│  主讲师看 USAGE_LOG.md                       │
│  - 谁在用 / 用得多频繁                         │
│  - 用了什么模板 / 用了多久                     │
│  - 有没有异常                                  │
└─────────────────────────────────────────────┘
```

**一句话总结**:`python3 generate_keys.py` → 复制话术 → 微信发送 → 同事激活 → 自动追踪。整套流程 1 分钟搞定,5 个文件零依赖。
